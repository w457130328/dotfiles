# 已掌握 i3 浏览与布局组织（0001–0002）

用户已完成前两课并做了练习：能独立完成开终端、切焦点、切工作区、关窗、系统菜单（0001），以及移动窗口、分屏、布局切换（0002）。通过两个追问展示了真正理解，而不只是覆盖：主动要 `Mod+z`/`Mod+v` 的记忆法，以及问 `Mod+w` 之后如何切回平铺。

关键澄清：用户的配置里 `workspace_layout tabbed`（config 第 10 行）使新工作区默认是标签页布局，一度让他困惑；已确认 `Mod+e`（`layout toggle split`）能从 tabbed 切回平铺，并补进速查表。

**Implications**: 用户对「新工作区默认 tabbed」这个默认值敏感——未来进入「改得动」阶段时，`workspace_layout` 是第一个可以切入的修改点。分屏方向已形成记法「v 往下，z 往右」，后续 layout 相关课程可直接复用，不必重教。
